export const CONFIG = Object.freeze({
  DEV: true,
  SHOW_DEBUG_CODE: true,
  /** When true, log pacing/timing events (do not enable in production). */
  DEBUG_PACE_LOGS: false,
  /** When true, show DEV-only SKIP button to instantly complete current dialogue sequence. */
  DEV_SKIP_DIALOGUE: true
});
